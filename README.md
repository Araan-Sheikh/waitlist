# is-host.in Waitlist

A minimal, modern waitlist site built with Next.js 16 and Tailwind CSS.

## Quickstart

1) Install deps

```bash
npm install
```

2) Run dev server

```bash
npm run dev
```

Open http://localhost:3000

Emails are stored locally at `data/waitlist.csv`.
